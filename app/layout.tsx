import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Clean Karachi - Report Issues, Track Resolution, Build Better City",
  description:
    "Join the Clean Karachi campaign to report garbage, sewage problems, and civic issues. Our platform connects citizens with authorities for real-time issue tracking and resolution. Together we can make Karachi cleaner.",
  keywords: "clean karachi, report garbage, civic issues, karachi cleanliness, community reporting, waste management",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}